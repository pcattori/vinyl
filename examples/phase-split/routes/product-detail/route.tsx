import { route } from "./.route"

export default route({
  meta: undefined,
  links: undefined,
  Component({ params, loaderData, actionData }) {
    return null
  },
  // ErrorBoundary() {},
  shouldRevalidate: undefined,
  handle: undefined,
})
